DATABASE_URL="postgresql://postgres:postgres@localhost:5432/oceantechai?schema=public"

# AI Provider (mock | openai | hybrid)
AI_PROVIDER_MODE=mock
OPENAI_API_KEY=
OPENAI_DEFAULT_MODEL=gpt-4o
AI_PROVIDER_TIMEOUT_MS=60000
AI_PROVIDER_MAX_RETRIES=2

# GitHub Integration (optional — app runs without these)
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
GITHUB_DEFAULT_BRANCH=main